const { User, validateUser } = require("./user");
const { Restaurant, validateRestaurant } = require("./restaurant");

module.exports = {
  User,
  validateUser,
  Restaurant,
  validateRestaurant,
};
